export const metadata = {
  title: 'Ask Carly | Support & Feedback',
  description: 'Share feature ideas or report issues. We read everything.',
  robots: {
    index: false,
    follow: true,
  },
};

export default function AskLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
